#include <iostream>
#include <cstdio>
#include <cmath>
#include <cstring>
using namespace std;
int n, sum = 0, a[21], used[21], f[10010], s[10010][22];
int main()
{
	scanf("%d", &n);
	for (int i = 1; i <= n; i++)
		scanf("%d", &a[i]), sum += a[i];
	each = sum / 4;
	
	memset(f, 0, sizeof(f));
	memset
	int f
	for ()
	
	return 0;
}
