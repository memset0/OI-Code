#include <cstdio>
#include <cstring>
#include <cmath>
#include <iostream>
using namespace std;
char k[100009];
bool mark[100009];
int main()
{
	int n, m;
	int min;
	scanf("%d%d%*c", &n, &m);
	for (int i = 1; i <= n; i++)
		scanf("%c", &k[i]);
	int _m = m;
	for (int i = 1; i <= n; i++)
	{
		
	}
	return 0;
} 
