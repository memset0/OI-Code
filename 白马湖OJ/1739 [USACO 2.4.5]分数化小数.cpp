#include <bits/stdc++.h> 
using namespace std;
const int maxn = 1010;
int n, m, t;
vector < int > a, b, c, O;
int main()  {
	cin >> n >> m;
	if (n > m) {
		t = n / m;
		while (t > 0) {
			a.push_back(t % 10);
			t /= 10;
		}
	} else a.push_back(0);
	while (1) {
		n = n * 10;
		b,push_back()
	}
}
