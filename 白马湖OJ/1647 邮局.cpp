#include <bits/stdc++.h>
using namespace std;
const int maxn = 310;
const int maxm = 40;
int n, m, a[maxn];
int main() {
	cin >> n >> m;
	for (int i = 1; i <= n; i++)
		scanf("%d", &a[i]);
	for (int i = 1; i <= n; i++) {
		
	}
}
