#include <iostream>
#include <cstdio>
#include <cstring>
#include <cmath>
#include <algorthm>
using namespace std;
const int maxn = 100000 + (1 << 3);
int a[maxn];
int n, x1, y1, x2, y2;
int main()
{
	scanf("%d%d%d%d%d", &x1, &y1, &x2, &y2, &n);
	for (int i = 1; i <= n; i++)
		scanf("%d", &a[i]); 
	
	return 0;
}
