#include <bits/stdc++.h> 
using namespace std;
const int maxn = 60;
int n, m, a[maxn][maxn];
int main() {
	cin >> n >> m;
	for (int i = 1; i <= n; i++)
		for (int j = 1; j <= m; j++)
			scanf("%d", &a[i][j]);
	
	return 0;
}
