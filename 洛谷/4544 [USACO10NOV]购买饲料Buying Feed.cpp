#include <bits/stdc++.h>
using namespace std;
int read() {
	char ch = getchar(); bool fl = 0; int rt = 0;
	while (!isdigit(ch) && ch != '-') ch = getchar();
	if (ch == '-') ch = getchar(), fl = 1;
	while (isdigit(ch)) rt = rt * 10 + ch - '0', ch = getchar();
	if (fl) rt = -rt;
	return rt;
}
int main() {
	
	
	
	return 0;
}
