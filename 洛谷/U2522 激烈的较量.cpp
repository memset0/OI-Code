#include <bits/stdc++.h>
using namespace std;
int n;
char a[100], b[100];
int main() {
//	scanf("%d%s%s", &n, a + 1, b + 1);
//	int cmp = 0;
////	for (int i = 1; i <= n; i++) {
////		if (a[i] == '>') cmp++;
////		else cmp--;
////		if (b[i] == '<') cmp++;
////		else cmp--;
////	}
//	for (int i = 1; i <= n; i++)
//		if (a[i] != b[i]) 
//			if (a[i] == '>') cmp++;
//			else cmp--;
//	if (cmp == 0) printf("Drew.\n");
//	else if (cmp > 0) printf("uim wins.\n");
//	else 
	printf("uim wins.");
	return 0;
}
